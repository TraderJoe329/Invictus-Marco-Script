if skill 'anatomy' > 90
    skill 'anatomy'
    waitfortarget 1000
    lasttarget
    wait 1000 
endif
if skill 'animallore' >= 90
    skill 'animallore'
    waitfortarget 1000
    lasttarget
    wait 1000
endif
if skill 'evaluatingintelligence' > 90
    skill 'evaluatingintelligence'
    waitfortarget 1000
    lasttarget 
    wait 1000
endif